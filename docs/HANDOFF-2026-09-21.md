# Handoff 2026-09-21 — scaffold pushed; next session drives CI green, then week 1

For a **Sonnet** session on any machine with `gh` and `pal clink` (codex + kimi). Read
`ai/STATE.md` first, then this. The judgment work is done and recorded; what remains this
week is execution against a written spec, which is why it does not need Fable.

## What this session (Fable, laptop) did

1. **Sol reviewed the plan** — `docs/reviews/SOL-PLAN-REVIEW-2026-09-21.md` (943 s, real
   content, cites file:line). Three blocking findings, all accepted: the SDK gate was
   underspecified (fixed in PLAN §4.2 and in the scaffold), fold→pause→resume is unsafe for a
   file being written (PLAN §1/§2/§4.5 now say *finish the segment*), and the ladder never
   touched a real recorder (new rung 4b on Perry's iPhone 16 Pro).
2. **Synthesis** — `docs/research/SYNTHESIS-2026-09.md`: every research and review claim
   graded ESTABLISHED / INFERRED / UNVERIFIED, Kimi 1/2/4/5 and Sol 1–6 adjudicated, seven
   declines in writing, and the PLAN edit list. Two first-hand checks reversed prior
   verdicts (touch on the outer display is *documented as possible*; the 27.1 beta *does*
   have the Duo simulator) — see STATE Open Loops. **Apple's DocC pages are readable via
   `https://developer.apple.com/tutorials/data/documentation/<framework>/<symbol>.json`**
   where the HTML returns only a title; use that pattern for any further Apple lookups.
3. **PLAN.md §1, 2, 4, 5, 7, 8, 9 edited once**, in the same commit as the synthesis
   (`b8e5902`).
4. **Scaffold written, cross-reviewed by Kimi (`docs/reviews/SCAFFOLD-REVIEW-2026-09-21.md`),
   pushed as `195da92`.** `project.yml` (three targets; the `STORYCUE_DUO_CONDITIONS`
   indirection scopes the flag to the app + `StoryCueDuoTests` only), `build.yml`
   (`macos-27`, two-fact gate, baseline lane always + Duo lane when SDK ≥ 27.1, `xcbeautify`,
   xcresult artifacts), `deploy.yml` (`run_number.run_attempt`, concurrency group,
   `manageAppVersionAndBuildNumber=false`, .ipa read-back, upload only on `v*` tags or the
   dispatch flag), `PrivacyInfo.xcprivacy`, camera/mic/photos usage strings, a two-file app
   (`StoryCueApp.swift`, `DuoSupport.swift`) and the two test bundles whose tests prove the
   flag-scoping invariant.
5. **Runner README re-read** — unchanged, no 27.1 SDK, no Duo simulator (STATE).

## CI state at handoff

**Unknown — the first run had not started.** Push `195da92` at 19:38Z triggered Build & Test
run **35646080336**; it was still `queued` on GitHub's `macos-27` pool 18 minutes later, and
a background watcher on the laptop was killed for memory pressure. So the scaffold has been
reviewed (Kimi) and YAML-parsed, but **never executed**. First thing next session:

```
gh run view 35646080336 -R pmartin1915/storycue --log-failed
```

Green means the week-0 gate is met and Task 1 collapses to the app icon. Red is expected
to be one of the first-run trouble spots listed under Task 1, and is fixed forward.

## Task 1 — get `build.yml` green on the empty app (the week-0 gate)

- `gh run list -R pmartin1915/storycue` → open the failing step's log → fix forward on
  `main` in small commits. Expected first-run trouble spots, in order of likelihood:
  XcodeGen semantics (`info.path`, `TEST_HOST`), the `-showBuildSettings | grep` evidence
  step being too strict, the simulator name (`iPhone 17` per the README), `xcbeautify` exit
  handling. **Do not "fix" a red by deleting the evidence steps or by widening the gate.**
- **Before iterating, check the Actions budget.** This is a private repo and macOS minutes
  bill at 10x against the account's shared pool; Shortless's `84176de` ("disable ... CI to
  stop Actions drain") is the precedent. `gh api /user/settings/billing/actions` shows
  minutes used and the spending limit (keep it $0). If the red→green loop needs more than
  ~3 runs, switch `build.yml`'s `on:` to `workflow_dispatch` + `pull_request` while
  iterating and restore `push` when green. A long `queued` can be a minutes/pool problem,
  not just `macos-27` demand — check that first.
- **Deployment target is iOS 27.0.** Rung 4b (week 3) assumes Perry's iPhone 16 Pro
  installs the TestFlight build, so confirm the phone is on iOS 27.x before week 3, or
  lower `deploymentTarget` in `project.yml` to 26.0 (Concentricity APIs are iOS 26, so
  nothing in the plan needs 27.0 as a floor).
- When green, read the job summary: it must say the SDK is 27.0, the flag is NOT SET, and
  the Duo simulator is absent. That is the correct green for today.
- Then the app icon: `DevProjects/brand` is the canonical source (`npm run check` there is
  the gate); export the 1024 px icon into `StoryCue/Assets.xcassets/AppIcon.appiconset/`
  and add it to `Contents.json`. Archive will not validate without it.

## Task 2 — week 1: the single-screen recorder

Spec inputs, all written: PLAN §2 (loop, segments, consent), §4.5 (the state-machine test
list — Sol 2's event table in `docs/reviews/SOL-PLAN-REVIEW-2026-09-21.md` finding 2 is the
authoritative enumeration), SYNTHESIS Q3 (camera surface: v1 uses the Virtual Front Camera
and the rear camera; **no** individual inner/outer device types, they do not exist on the
27.0 SDK), SYNTHESIS Q7 (product rules: pause = finish segment, count-up timer, 10-min
segment cap, next-question preview, outer type 48–60 pt / 4–6 words a line, original copy
only, VHP questions may ship verbatim, read-aloud consent line, general audience).

Shape, so the pieces land in the order CI can judge them:

1. `SessionMachine` (pure Swift, no AVFoundation import): deck × recording × segment
   finalization × hinge × scene phase × export manifest. Every transition in Sol's table
   ends in `finishing` → `segmentSaved` | `segmentFailed(kept:)`; nothing resumes a write.
   XCTest in `StoryCueTests` covers every row. **Gate: green.**
2. `CaptureService` protocol + `MockCaptureService` (drives the machine in tests) +
   `AVCaptureService` (real; `AVCaptureMovieFileOutput`, finish delegate, interruption and
   runtime-error notifications, background task around a recording, durable segment ledger
   in Application Support for crash recovery). **Gate: green** (the real service compiles;
   only the mock runs in CI).
3. Decks as Swift data (`Deck`, `Question`, `isIncluded` bit), five v1 decks with original
   copy; a free-tier `glm-4-flash` / `gemini-2.5-flash` pass on tone is Routine.
4. Inner UI (preview, current question, count-up timer, next/skip, pause/resume, consent
   card) and the **outer-preview debug panel** (rung 4) with a UI test that writes
   `simctl` screenshots as artifacts.
5. Session library + export to Files / Photos (stitching is 1.x; clips-as-files is v1).

**Routing:** steps 1–3 are bulk implementation against this spec → `/orchestrate` (Kimi
executor, Sol audits the diff — executor is never its own reviewer). Name the exported
types and the test file names in the spec, because Kimi's tests otherwise re-implement the
product. Steps 4–5 are UI-shaped and can stay on Sonnet or go to `/orchestrate`; the
`aistudio-build` lane does not apply (native Swift). **Nothing here is clinical**, so the
standing commit-and-push authorization covers it.

Then **Kimi on the state machine** (PLAN §8 row 4, the paste-ready prompt) once step 1
exists, and **Sol on the recorder diff** at the week-1 gate.

## Task 3 — housekeeping at the end

- Update `ai/STATE.md`; write `docs/HANDOFF-<date>.md`; `node dev-ops/scripts/session-claim.mjs
  release` (this session's claim was `perryslenovo-storycue`).
- If the runner README changes (`images/macos/xcode-27-arm64-Readme.md`), record the date
  and the new SDK list in STATE and re-run CI: the Duo lane turns on by itself.

## Do not

- Write Duo-path code (accessory, hinge, direction coordinator) this week. It is week 2,
  under the gate, and every Duo diff goes to Sol.
- Widen `deploy.yml`'s secret set beyond Shortless's six + `PROVISIONING_PROFILE_APP`; the
  secrets, App ID, profile and ASC record are Perry's (PLAN §7). Do not run `deploy.yml`
  until they exist.
- Rename `deploy.yml` (resets `run_number`), re-run a deploy past upload, or bump
  `CURRENT_PROJECT_VERSION` by hand.
- Reopen the name, the no-hardware decision, or v1 display-only — all recorded, with reasons.
- Add attribution trailers to commits (global rule).
