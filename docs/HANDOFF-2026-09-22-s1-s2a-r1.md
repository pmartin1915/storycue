# Handoff 2026-09-22 evening (S1 merged; S2a and R1 built, both waiting on CI)

Governing order is still `docs/STRATEGY-2026-09-22.md`. This session ran the loop: Opus specs,
Kimi builds, Sol audits, boss adjudicates.

## The blocker: GitHub Actions billing (Perry only)

From ~8:24 PM CT every Build & Test job was refused before it started: *"The job was not
started because recent account payments have failed or your spending limit needs to be
increased."* It's account-wide, so both repos are affected. Nothing Swift can be compiled until
Perry fixes it in GitHub → Settings → Billing & plans. No session should touch billing (Money
Rule). Once it's fixed:

1. storycue PR #3 (`s2a-impl`): `gh run rerun` the latest run on the branch, or push an empty
   commit. Head is `6628a23`.
2. retold PR #1 (`r1-impl`): the same. Head is `f590446`.

## StoryCue

- **S1 merged**: PR #2, squash `052a3ee`. CI 89/89 on both lanes (run `35803576408`). Sol's
  audit (`docs/reviews/S1-SOL-AUDIT-2026-09-22.md`) found two High wedges, both fixed: a failed
  capture start, and a media-services reset while recording, each used to leave the session
  stuck in `.finishing`. That same run was the Duo lane's first under the S0 selector. The
  baseline used GA `Xcode_27.app`, which passed the allowlist; the Duo lane used
  `Xcode_27.2_beta.app`.
- **S2a built, not compiled**: branch `s2a-impl`, draft PR #3. Kimi implemented the whole spec
  (`ddc0f25`, 32 new tests). Kimi's layout review (`docs/reviews/S2A-LAYOUT-REVIEW-2026-09-22.md`)
  produced 3 accepted fixes in `f3f7796`. **Still to do once CI is green:** fix whatever CI
  reports, then a Sol diff audit (base `052a3ee`), then merge. Expected compile risks:
  `CameraPreview.swift` (already `@preconcurrency import AVFoundation`), the `RecorderView`
  countdown `Task` touching `@State`, and `UICopyTests.testNoCopyContainsTODO` reading a source
  file through `#filePath` from the simulator.
- **Next spec: S2b (library)**. It depends on S2a's `AppModel`, so write it after S2a compiles.
  S3 (export) and S4 (deck copy) can be specced in parallel.

## Retold

- **R1 built, not compiled**: branch `r1-impl`, draft PR #1. Kimi implemented it (`518a304`).
  Sol's Rule-1 audit (`docs/reviews/R1-SOL-AUDIT-2026-09-22.md`) found a **real Rule-1 hole**:
  an episode title could be model text confirmed by a tap. Fixed in `f590446`: titles are now
  verified quotes or typed by the user, and nothing else is constructible. Four more findings
  are deferred to R2/R7 in `ai/IDEAS.md`. The R2 spec must make `VerifiedSpan` constructible
  only through the verifier.
- **Reading R1's first CI run is the spike**: `testDetailStoredProvenanceSurvivesReopen` green
  while `testConfirmableIntSurvivesReopen` is red means SwiftData can't persist the generic.
  The pre-registered fallback is concrete `ConfirmableInt` / `ConfirmableString` through a
  follow-up spec. Any other red is a normal fix-forward.

## Environment notes

- Claude Code's low-memory reaper killed background shells twice (Chrome was using ~6.7 GB, plus
  19 Claude processes). The Kimi child outlives its killed wrapper, so judge a run by its
  worktree, not its log. That's logged in `~/.claude/PAPERCUTS.md`. Launching with
  `CLAUDE_CODE_DISABLE_BG_SHELL_PRESSURE_REAP=1` turns the reaper off.
- Sol ran out of ChatGPT Plus quota at ~3:30 PM (reset 4:46 PM). Each diff audit costs about
  0.9M input tokens, so plan one Sol audit per diff, not per commit.

## Still Perry's (unchanged from the strategy)

- Apple setup for both apps, hard latest 10-03: certificate expiry, App IDs, App Store profiles,
  ASC records, GitHub secrets.
- The GitHub billing fix above, which now gates everything.
