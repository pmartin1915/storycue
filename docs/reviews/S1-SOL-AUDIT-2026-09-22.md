# S1 diff audit — Sol, 2026-09-22

Reviewer: Sol (GPT-5 via `pal clink`, `codex`, codereviewer role; 755 s). Input: `git diff
b6c4c84..790aff6 -- StoryCue StoryCueTests`, the S1 spec, and the pre-change reducer. No commit
messages were passed, so the review wasn't steered toward the boss's own fixes. Six findings plus
one INSUFFICIENT_CONTEXT.

| # | Finding | Verdict | Action |
|---|---|---|---|
| 1 | High: a failed `startSegment` sends `.runtimeError` → `.finishing` + stop, but capture never started, so no callback comes; the session wedges and the ledger entry stays `.writing` | **accept** | Store reports the failed start as `fileOutputFinished(id, .failed(kept: false))`: from `.recording` the S1 §3b branch pauses with no stop; from `.finishing` it finishes normally. Test `testStartFailurePausesWithoutStop` replaces `testStartFailureFeedsRuntimeError` (which had encoded the wedge) |
| 2 | High: on media-services reset the reducer queues stop + recreate; recreation drops the old output/delegate, so the finish callback may never arrive and the session wedges in `.finishing` | **accept** | After the recreate effect, if still `.finishing(_, .mediaServicesReset)`, the store sends `.runtimeError`, the reducer's existing escape hatch (`.failed(kept: true)`, pauses with the original reason). A late real callback is ignored as stale. Test `testMediaServicesResetWhileRecordingDoesNotWedge` |
| 3 | Medium: `hasAudioInput` not refreshed after a successful recreate | **accept** | Re-read after recreate. Test `testRecreateRefreshesAudioInput` |
| 4 | Medium: per-session notification listeners start just before `startRunning()` with no readiness barrier | **defer** | Low probability; needs an injectable notification source. `ai/IDEAS.md` |
| 5 | Low: ticker task strongly captures the store, which owns the task | **accept** | Weak capture per iteration |
| 6 | Low: the ledger-before-capture test can't observe order | **defer** | Correct by inspection; the boss flagged the same gap before this audit. `ai/IDEAS.md` |
| IC | Invariant 4 can't be enforced by `SegmentFiles` alone; the S2 caller must inject the Application Support default | **noted** | S2a spec's `AppModel.production()` does exactly that |

Sol's summary also confirmed: the FIFO consumer is sound, the reducer changes are additive, early
matching callbacks don't auto-resume, ledger writes precede capture in the implementation,
segments default to Application Support, and the forbidden constructs are absent.
