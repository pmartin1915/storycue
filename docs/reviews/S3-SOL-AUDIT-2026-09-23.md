# S3 Sol diff audit — 2026-09-23

Base `00aa2b0`, head `66acbd4` (CI green, 133/133 on both lanes, `AVStitcherTests` really ran and weren't skipped). Sol's raw output is in `.orchestrate/logs/20260923T210356Z-sol-implement.final.txt`, which isn't committed. The first attempt was killed by Claude Code's low-memory reaper, and this is the rerun. Sol reported 7 findings: 6 were accepted and fixed in the same commit as this file, and 1 was rejected.

| # | Sol severity | Finding | Verdict |
|---|---|---|---|
| 1 | blocking | In per-clip mode, two question IDs that aren't in the deck both got `Q00`, so the second stitch overwrote the first file | **Accepted.** The planner now makes per-clip names unique by adding ` (2)`, ` (3)` and so on. New test: `testDuplicateFileNamesAreMadeUnique`. |
| 2 | major | `AVStitcher`'s load `catch` turned a cancellation into `.unreadable` | **Accepted.** A cancelled load rethrows `CancellationError`, and the loop checks for cancellation before each source. |
| 3 | major | When every source was unreadable, it returned before removing a stale file at `output` | **Accepted.** `output` is now removed first, before any source is loaded. |
| 4 | minor | The unreadable→segment lookup covered every manifest segment, not just the planned sources, so a segment the planner had already dropped could be counted twice | **Accepted.** The lookup now covers planned sources only, and anything else takes the `assertionFailure` path. |
| 5 | minor | The Photos-denied copy uses `›` and not `>` | **Rejected.** The spec's own §5 copy uses `›`. |
| 6 | minor | `testExportNeverTouchesSegmentDirectory` compared only file names | **Accepted.** The snapshot now includes size and modification time. |
| 7 | minor | `testFilesExportWritesOneFilePerClip` used two separate `Date()` calls, so it was flaky across midnight | **Accepted.** It uses one captured session date. |
