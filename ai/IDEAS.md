# IDEAS -- storycue (append-only; not @-imported)

One line each: idea, why deferred, where it applies. Sweep at session start.

- 2026-09-21 (Sol 6) App Store Connect pre-query in deploy.yml for an existing bundle-id/version/build before archiving -- deferred: needs an authenticated ASC API call in the job for a collision that run_number.run_attempt + "never re-run past upload" + the concurrency group already prevent for a solo dev; applies to deploy.yml if a duplicate-build 409 ever happens anyway.
- 2026-09-21 (PROMPT-B section 6) bundle Atkinson Hyperlegible (OFL) for the outer-display prompt text -- deferred: a Kimi layout-review question for week 2, not a v1 requirement; SF Pro at heavy weights is the fallback; applies to the outer/accessory view.
- 2026-09-21 the "Duo SDK gate" step is duplicated in build.yml and deploy.yml -- deferred: extract to a composite action under .github/actions/ once both have run green at least once and the printed facts have stabilised; applies to both workflows.
- 2026-09-21 (Sol 5) privacy-safe diagnostic export (scene phase, hinge state, accessory availability, interruptions, device descriptor, file-output completion, all timestamps, local file, user-initiated share) -- deferred to week 2 under the gate; applies to the Duo path and to rung-5 tester reports.
- 2026-09-21 (PROMPT-B section 5) free download + one-time unlock via StoreKit 2 -- Perry decision; v1 ships free with all decks; the deck model carries an isIncluded bit from day one so this is additive; applies to the deck model and ASC pricing.
- 2026-09-22 (advisor, week1 spec review) nothing in SessionEffect emits .persistLedger(.writing) on segment start -- only on finish -- so SegmentLedger.record() has no reducer-driven caller for the .writing state a crash needs to detect; deferred: it's a SessionStore (step 4) wiring gap, not a week-1 SessionMachine/SegmentLedger defect -- both types are correct and tested standalone; applies to SessionStore's design when step 4 starts.
- 2026-09-22 (Sol week1 diff audit, finding 2) AVCaptureService.configureSession()'s `if configured { return }` guard (line 69) never gets flipped back to false, so it can't honor a `.recreateCaptureSession` effect after mediaServicesReset -- fix is to set `configured = false` and nil out captureSession/movieFileOutput/recordingDelegate/pressureObservation in handleRuntimeError when the reset case fires; deferred: unreachable this week, no SessionStore exists yet to call configureSession() again in response to the effect; applies to AVCaptureService when step 4 (SessionStore) wires effects to real calls.
- 2026-09-22 (Sol week1 diff audit, finding 6) AVCaptureService.init() starts four unstructured Task{ for await ... } loops against NotificationCenter.default (lines 37-64), never stored or cancelled, strongly retaining the actor for app lifetime and not scoped to this instance's own capture session -- low risk while only one instance ever exists, but would cross-talk if a second instance were ever created (e.g. a future SessionStore recreating the service on reset per the item above); applies to AVCaptureService, revisit alongside the configureSession() fix.
- 2026-09-22 (Sol week1 diff audit, finding 9, rejected as a defect but noting the gap) MockCaptureService.RecordedStart and AVCaptureService's private MovieRecordingDelegate are internal helper types not literally named in docs/WEEK1-SPEC.md -- judged not a spec violation (the spec only left "record calls" unspecified in shape for MockCaptureService, and MovieRecordingDelegate is implementation plumbing required by AVFoundation's NSObject-based delegate protocol, not a domain type); no action needed, logged so a future spec-compliance pass doesn't re-flag it as new.
- 2026-09-22 (Kimi state-machine transition gap analysis, .orchestrate/logs/20260922T055152Z-implement.log) 13 (phase x event) corners the 35-row acceptance table doesn't test even though SessionMachine's code already handles them correctly on inspection: (.finishing + mediaServicesReset unchanged-phase branch), (.finishing[.userStop or plain runtimeError-escape] + fileOutputFinished/runtimeError with backgroundTaskActive==true, i.e. background-task-ending combined with the non-.sceneBackgrounded finish exits), (.finishing + fileOutputFinished with a segmentID absent from state.clips -- phase still transitions but no persistLedger fires), plus several lower-risk no-op guard corners (hingeChanged from .idle/.paused, accessoryAvailabilityChanged(true), runtimeError from .idle/.paused, tap-guard rejections outside the .finishing case). None are code bugs; all are missing test methods. **RESOLVED 2026-09-22**: all 13 gaps now covered by 10 new test methods (docs/WEEK1-COVERAGE-SPEC.md, dispatched via /orchestrate, Sol audit zero findings, CI green 59/59 both lanes on `68037c9`) -- see ai/STATE.md.
- 2026-09-22 (advisor, week1 diff review) if Apple's real AVCaptureMovieFileOutput delegate fires *before* the interruption/runtimeError notification that caused it (ordering isn't guaranteed), fileOutputFinished arrives while the reducer is still .recording and the spec'd reducer has no handler for that state -- the event is effectively dropped and the reducer then transitions to .finishing on the later notification, waiting for a callback that already came. Spec-compliant as written; only verifiable on real hardware, not on this non-macOS CI host; applies to AVCaptureService/SessionMachine interaction, revisit once a Duo device or any physical iPhone test rig exists.
- 2026-09-22 (Sol strategy review, finding 8) poll the App Store Connect API after `altool --upload-app` until the build's processing state is VALID or FAILED, so a deploy can't look green while ASC rejects the build -- deferred: needs an authenticated ASC API loop in the job; Perry sees processing state in ASC anyway; applies to deploy.yml in both storycue and retold.
- 2026-09-22 (strategy session) the early-`fileOutputFinished` race logged above is **scheduled into S1** (`docs/S1-SESSIONSTORE-SPEC.md` §3b) -- no longer deferred.
- 2026-09-22 (Sol S1 diff audit, deferred): AVCaptureService starts its three per-session notification listener tasks just before the synchronous `startRunning()`, with no readiness handshake, so an interruption/runtime error posted during startup could be missed. Low probability; fix needs an injectable notification source + readiness barrier. Where: `startCaptureNotificationTasks(for:)`.
- 2026-09-22 (Sol S1 finding 6, deferred): `testTapRecordRecordsLedgerWritingBeforeCaptureStart` can't observe ledger-before-capture order (separate actors). Correct by inspection in `SessionStore.execute(.startSegment)`. A shared ordered probe would make it testable.
- 2026-09-23 (Sol S2a audit, finding 3, rejected for 1.0) the recorder never re-reads camera/mic authorization or `hasAudioInput` after a return from Settings. That's rejected because iOS terminates an app when its camera or microphone privacy permission changes, so the stale state shouldn't be reachable. Check on device at S5: deny, then Open Settings, grant, and return. If the app is NOT relaunched, call `store.prepareCapture()` on `.sceneDidBecomeActive` while `blocking == .notAuthorized`. Applies to `RecorderView`/`SessionStore`.
