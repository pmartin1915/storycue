# STATE -- storycue

> Seeded 2026-09-18 by hand from dev-ops branch claude/ios-duo-app-ideas-te3edu: the four docs
> copied with `git show` and byte-matched via `cmp`, seed files written directly. The branch's
> bootstrap-storycue.ps1 was NOT executed (the session's classifier refused it) and has run on
> no machine; its -DevOps default resolves wrong when run from outside dev-ops. Keep short; read first.

## What this is
See README.md and docs/PLAN.md. Decisions of record are in docs/HANDOFF-2026-09-18.md
(name "StoryCue" for now; NO Duo hardware; identifiers and pipeline as Shortless).
**docs/PLAN.md was edited in one coordinated pass on 2026-09-21** from
docs/research/SYNTHESIS-2026-09.md, which is where every research/review verdict lives.

## What's Done
- [x] Plan, handoff, two Gemini research prompts (2026-09-18)
- [x] Local folder seeded (this file)
- [x] Kimi plan review, adjudicated: docs/reviews/PLAN-REVIEW-2026-09-18.md (3 accept, 1 reject-with-replacement)
- [x] GitHub remote pmartin1915/storycue (private), main pushed 2026-09-18; dev-ops registry row added (on dev-ops master, 92d29df)
- [x] Gemini Deep Research reports landed 2026-09-18: docs/research/PROMPT-A-ship-path-REPORT.md, PROMPT-B-product-REPORT.md
- [x] Tech Talk transcripts landed 2026-09-18: docs/techtalks/111461, 111463, 111464, 111465
- [x] Reference fixes 2026-09-19 (commit 6d0c3f7)
- [x] **Sol plan review RAN 2026-09-21** (943 s, return_code 0, read the files): docs/reviews/SOL-PLAN-REVIEW-2026-09-21.md. 3 blocking + 3 should-fix; all adjudicated in SYNTHESIS §S (5 accept, 1 partial, 1 deferred to IDEAS).
- [x] **Synthesis 2026-09-21**: docs/research/SYNTHESIS-2026-09.md folds both Gemini reports, all four transcripts, Kimi 1/2/4/5 and Sol 1-6, plus two first-hand checks that changed verdicts (below). PLAN.md sections 1, 2, 4, 5, 7, 8, 9 edited in the same pass.
- [x] Runner README re-read 2026-09-21: unchanged since 09-18 (image 20260912.0186.1, Xcode 27.0, no 27.1 SDK, no Duo simulator). Image has xcbeautify 3.2.1 and xcodes; NOT xcodegen or xcpretty.

- [x] **Scaffold written, Kimi-reviewed (docs/reviews/SCAFFOLD-REVIEW-2026-09-21.md), pushed as 195da92** -- project.yml, build.yml, deploy.yml, PrivacyInfo.xcprivacy, usage strings, DuoSupport + two test bundles.
- [x] **Week-0 gate MET 2026-09-22: CI is green** (run 35676600490, conclusion success). Confirmed the correct baseline-only green: iphoneos SDK 27.0, Duo SDK headers NO (flag NOT SET), Duo simulator device type NO -- exactly the expected facts for today's runner image.
  - Root cause of every prior run never even starting (all showed `runner_id:0`, never `in_progress`): **`runs-on: macos-27` is not a real GitHub-hosted runner label.** Confirmed against `actions/runner-images`'s published catalog -- the Xcode 27 preview image's label is `xcode-27` (fixed in build.yml and deploy.yml, commit 994e577). This was NOT a billing/minutes problem; the Actions spending limit was never touched or queried.
  - Two further build-config bugs found by actually running it, fixed forward in small commits (3 executing runs total, within budget):
    1. `994e577` -- runner label fix (macos-27 -> xcode-27).
    2. `84378e7` -- `StoryCueTests` and `StoryCueDuoTests` both left `PRODUCT_NAME` unset in project.yml, so both resolved to an empty product name and collided on the same `PlugIns/.xctest` output path when the scheme built both together ("Multiple commands produce..."). Fixed by giving each an explicit `PRODUCT_NAME`.
    3. `30696c0` -- the app module was built without `-enable-testing`, so `StoryCueTests` couldn't `@testable import StoryCue` ("Unable to resolve Swift module dependency"). A hand-authored XcodeGen project doesn't inherit the `ENABLE_TESTABILITY=YES` default that an Xcode-created project's template bakes into its Debug config -- added explicitly under `settings.configs.Debug` in project.yml.
  - `build.yml`'s `push:` trigger was never removed -- green landed within the 3-run budget, no need to switch to workflow_dispatch-only iteration.
- [x] **Week 1 spec written 2026-09-22: docs/WEEK1-SPEC.md.** Names every exported type and test file for the single-screen recorder so `/orchestrate` doesn't have to invent them: `SessionMachine` (pure reducer, no AVFoundation import), `CaptureService`/`MockCaptureService`/`AVCaptureService` (actor-based, Swift 6 strict concurrency), `SegmentLedger` (crash-recovery ledger), deck data (`Deck`/`Question`/`Decks`), `ExportManifest`. Full acceptance table (**35 rows**, not the 20 this line used to say -- the table grew across the three post-dispatch fix commits af8f6e8/8f1a494/11cf380) built from Sol's event table plus PLAN section 4 item 5's "deck index, timer, export manifest" line. Deck copy is explicitly scoped OUT (placeholder `// TODO copy` text; real copy is a later Routine-tier pass). Nil-safety-at-init and the Duo-line exclusion (no direction-coordinator code in this week's `AVCaptureService`) are named as spec requirements, not implementation details, to avoid CI failing for reasons unrelated to the state machine.
- [x] **Week 1 implementation dispatched and DONE 2026-09-22.** `/orchestrate` ran (three implement attempts; the first two hit tooling issues -- a cross-session `.orchestrate/spec.md` collision, unrelated shell/process hazards a peer session is writing up separately -- the third succeeded, exit 0). Kimi wrote all 13 named files (~1470 lines) into `.orchestrate/wt/week1` on branch `week1-impl`, matching every one of the 35 acceptance-table test names exactly (zero invented, zero missing) plus all 11 other named tests across the other four test files, and every "Do not" invariant held (no `AVFoundation` import in the reducer, `MockCaptureService` fully `#if DEBUG`-gated, no Duo code outside the gate, no eager construction, `project.yml`/`StoryCueApp.swift` untouched, no AI attribution). Committed as WIP on `week1-impl` (durability, since the files arrived untracked and a spec.md collision had already happened once this session): `e6ec9e0`.
- [x] **Boss review done 2026-09-22: Sol audit + Kimi transition-gap analysis, both adjudicated, real findings fixed.** Own read of `SessionMachine.swift`/`ExportManifest.swift`/`SegmentLedger.swift` against all 35 rows found no discrepancies. Sol's decorrelated diff audit (`.orchestrate/logs/20260922T055148Z-sol-implement.final.txt`) found a real actor-state race in `AVCaptureService`: a single mutable `activeSegmentID` couldn't distinguish two recordings across the reducer's `runtimeError` escape hatch (rows 20/21), and tracing it further showed the bug was worse than reported -- the late callback for the old segment didn't just mislabel itself, it nil'd the property, silently dropping the *new* segment's own completion and wedging it. Fixed by deriving the segment ID from the recording's own file URL instead of actor-mutable state. Also fixed: a duplicate `.mediaServicesReset` emission (two sources reporting the same reset) and a manifest-order test whose fixture couldn't distinguish "preserves array order" from "sorts by questionID" (three more Sol findings -- non-idempotent `configureSession()` on reset, unbounded/uncancelled NotificationCenter tasks, two internal helper types not literally in the spec -- were judged real-but-out-of-scope-this-week or not-a-violation and logged to `ai/IDEAS.md` instead of fixed now). Kimi's parallel transition-gap analysis found 13 untested (phase x event) corners in the reducer, all confirmed as test gaps only (code already handles them correctly) -- logged to `ai/IDEAS.md` as a future coverage-hardening pass, not blocking. Fix commit: `0dd159e` on `week1-impl`.
- [x] **Week-1 gate MET 2026-09-22: pushed, PR #1 opened, CI green after three fix-forward rounds.** Perry chose "push + open a PR" when asked (the branch had never been pushed before). First compile of ~1,500 lines of Swift 6 strict-concurrency code written blind on a non-Mac host predictably needed iteration -- three real, distinct bugs found and fixed, none flakes:
  1. `51fbeb7` -- three `Notification`-is-not-`Sendable` errors in `AVCaptureService`'s `NotificationCenter` consumers (`SendingRisksDataRace`); fixed by extracting the one Sendable value each handler needs before the `await` hop into the actor, instead of passing the whole `Notification` across.
  2. `9622535` -- two `SessionMachineTests` "finish-then-advance" tests passed the `(SessionState, [SessionEffect])` tuple from a first `reduce()` call straight into a second `reduce()` call's `SessionState` parameter instead of unpacking `.0` (the pattern used correctly everywhere else in the file).
  3. `1042f22` (`week1-impl`) / `e735199` (`main`) -- **pre-existing scaffold gap, not a week-1 bug**: `project.yml` never set `SWIFT_ACTIVE_COMPILATION_CONDITIONS: DEBUG` for the Debug config (same class of gap as the `ENABLE_TESTABILITY` fix in `30696c0` -- a hand-authored XcodeGen project doesn't inherit Xcode's project-template defaults). `#if DEBUG` never activated, so `MockCaptureService.swift` (entirely `#if DEBUG`-gated) failed to compile -- the first week any file used that gate, so the gap had never surfaced before. Fixed on both branches since it's repo-wide, not implementation-specific.
  Final green run: `35694965403` -- `Build & test (baseline, flag off)`: 49/49 tests passed, including all 35 `SessionMachineTests` acceptance rows. `Build & test (Duo, flag on)`: skipped, correctly (this runner has no iOS 27.1 SDK). PR: https://github.com/pmartin1915/storycue/pull/1.
- [x] **PR #1 squash-merged to `main` 2026-09-22: `7b93894`.** Perry chose "merge now, squash" when asked directly. Local `main` fast-forwarded from `afb3e0a` to `7b93894`; `week1-impl` left intact (repo has `deleteBranchOnMerge: false`). Week 1 (single-screen recorder) is now on `main`.
- [x] **Coverage-hardening pass done 2026-09-22: `68037c9`.** All 13 (phase x event) gaps `ai/IDEAS.md` logged from the prior Kimi transition-gap analysis are now covered: `docs/WEEK1-COVERAGE-SPEC.md` named all 10 test methods (some cover multiple gaps) with exact setup/assertions derived from reading the reducer directly, dispatched via `/orchestrate` (Kimi executor). Test-only -- `SessionMachine.swift` untouched, confirmed by `git diff --stat` (one file, +128/-0). Sol's decorrelated audit (correlation guard: Kimi is the executor) found **zero findings**, independently re-verified all 10 tests' expected values against the reducer, confirmed the diff shape. Own manual re-check against `SessionMachine.swift` before dispatch and again against the diff after -- no discrepancies. Cherry-picked onto `main` as `68037c9`. **CI green, run `35759964133`: 59/59 tests both lanes** (49 prior + 10 new).
  - **Notable, unrelated to this pass: the Duo lane actually ran this time, not skipped.** `Duo SDK gate` step reported `iphoneos SDK version: 27.2`, `Duo SDK headers: YES`, `Duo simulator device type present: YES` (`DEVELOPER_DIR: /Applications/Xcode_27.2_beta.app`) -- the hosted runner image was updated since the last check (2026-09-21, SDK 27.0, no Duo). See Open Loops below; this changes a load-bearing fact for the 2026-10-10 pivot check.
- [x] **App icon done and shipped 2026-09-22.** `marks/apps/storycue.svg` written, gated, committed, pushed in the `brand` repo (`e8a29f6`). Perry picked "Quote-wave" from the four Gemini concepts (reconstructed from `docs/HANDOFF-2026-09-22-coverage-and-icon.md` + `brand/BRAND.md` in this session, since the original prompts were never saved to a file). Perry then generated the four on Gemini himself and confirmed Quote-wave as the winner. Hand-authored the glyph in three rounds (44-viewBox, stroke-only, rust `#E0935F`, no container -- matches `wilderness.svg`/`frontier.svg` convention): round 1 (open arc + crossing zigzag) rendered as an unreadable tangle; round 2 (closed circle + small crossing zigzag) read as a magnifying glass; round 3 -- closed loop, comma tail, EKG-style zigzag trailing off the loop's right edge with no overlap -- reads cleanly as "quote mark opening into a soundwave" at both 1024px and 192px. Passed `brand`'s `check-icons.mjs` clean (92% of the Android safe-circle radius, no baked iOS corner radius, no alpha on the marketing icon, all 18 iOS slots present); `brand`'s `npm run check` (contrast + drift) also run clean, no regressions. **Wired into the actual app**: the generated 18-slot iOS appiconset copied from `brand/dist/icons/apps/storycue/ios/AppIcon.appiconset/` into `StoryCue/Assets.xcassets/AppIcon.appiconset/`, replacing the Xcode-default empty placeholder (`9ac69db`). `project.yml` already pointed `ASSETCATALOG_COMPILER_APPICON_NAME`/`CFBundleIconName` at `AppIcon`, so no scaffold change needed. **CI confirmed green on the wiring commit**: run `35769450879`, conclusion success -- the icon change doesn't break the build.

## What's Next
- [ ] 2026-10-10 pivot check (plan section 5) -- **re-evaluate this date now that Open Loops' SDK-availability fact has changed** (see below); the EXPECTED path was 1.0 without Duo, written when the runner had no 27.1 SDK.

## Open Loops
- **CORRECTED 2026-09-22 (run `35759964133`): the hosted runner now HAS the Duo SDK and Duo simulator.** `iphoneos SDK 27.2` (Xcode 27.2 beta), `DUO_SDK` gate = YES, Duo simulator device type present = YES -- `StoryCueDuoTests` actually compiled and ran (not skipped) for the first time, 59/59 passing. This supersedes the 2026-09-21 recheck below and the "PROMPT-A expects hosted 27.1 late Oct / early Nov" estimate -- the runner beat that estimate by 5-6 weeks. Does NOT by itself mean Duo hardware-dependent code should start now (that's still the deliberate week-2-under-the-gate scoping decision, plan section 2/PLAN.md, unrelated to CI availability) -- but it does mean the 2026-10-10 pivot check's premise ("runner won't have it in time") needs re-reading before that date, not assumed. Not yet confirmed whether this is the new steady-state image or a transient beta rollout -- re-check on the next CI run before treating it as permanent.
- Prior fact, now superseded by the line above: the hosted runner had no iOS 27.1 SDK as of 2026-09-21.
- **Outer-display touch: CORRECTED 2026-09-21.** The earlier note here said "RESOLVED non-interactive by 111465 + PROMPT-A". Wrong on both counts: no transcript says anything about interactivity, and PROMPT-A quoted the UIKit *external display* article. Apple's DocC for CameraCaptureAccessory (read directly via the DocC JSON endpoint, which works where the HTML page returns only a title) says "unlike ExternalNonInteractiveAccessory, the content can be interactive." v1 is display-only BY DECISION (no hardware to verify hit-testing), not by platform limit. SYNTHESIS Q1.
- **Xcode 27.1 / Duo simulator contradiction: RESOLVED 2026-09-21.** PROMPT-A's "later this month" line is from the Xcode 27.2 beta notes (as its own footnote says); Apple forum 847137's accepted answer says the Duo simulator shows up in 27.1 beta after re-downloading the iOS 27.1 simulator runtime component. Press was right. Moot anyway: no Mac. Apple's own 27.1 release-notes HTML still unreadable (title only) -- try the DocC JSON endpoint pattern next time.
- Name: PROMPT-B recommends abandoning "StoryCue" but says its trademark search was SIMULATED. No evidence weight. Perry's call; locked "for now".
- Pricing: free + one-time unlock (PROMPT-B) is a Perry decision, deferred; deck model gets an `isIncluded` bit from the start.
- Optional operator act 2026-09-23: Apple online Duo Q&A (plan section 7 item 7).
- Sol's ASC pre-query for build-number collisions: deferred to ai/IDEAS.md.

## Git state
- Branch: main, remote https://github.com/pmartin1915/storycue.git (private). 2026-09-21 session: b8e5902 (synthesis + Sol review + PLAN pass), 195da92 (scaffold), 4d99fe3 + 332af19 (handoff, STATE, build.yml concurrency). 2026-09-22 session: 994e577 (runs-on label fix), 84378e7 (PRODUCT_NAME fix), 30696c0 (ENABLE_TESTABILITY fix) -- CI green on 30696c0 (run 35676600490). 2026-09-22 session (spec pass): docs/WEEK1-SPEC.md + this STATE update + docs/HANDOFF-2026-09-22-week1-spec.md, committed on top of 30696c0. 2026-09-22 session (implement+review pass): `4a22aee` (docs: handoff + STATE + IDEAS), `e735199` (build: SWIFT_ACTIVE_COMPILATION_CONDITIONS fix, applies repo-wide not just to week1-impl) -- main's own CI green on `e735199` (run 35694941266). 2026-09-22 session (merge pass): PR #1 squash-merged, `7b93894` (Week 1: single-screen recorder). 2026-09-22 session (coverage pass): `744eca7` + `a552d38` (docs: coverage spec + escalation clause), `68037c9` (test: 10 coverage-hardening methods, cherry-picked from the dispatch worktree) -- CI green (run 35759964133, 59/59 both lanes, Duo lane actually ran). 2026-09-22 session (icon pass): `60213ac` (docs: icon glyph committed to brand, handoff), `9ac69db` (ui: wire the generated app icon into the asset catalog) -- CI green on the wiring commit (run 35769450879). Companion commit in the sibling `brand` repo: `e8a29f6` ("brand: StoryCue joins the outdoor/life line"). **main is at `9ac69db`.**
- Branch: `week1-impl`, remote https://github.com/pmartin1915/storycue.git. **Merged into main via PR #1 (squash, `7b93894`), left in place** (`deleteBranchOnMerge: false` -- not deleted, just superseded). Worktree at `.orchestrate/wt/week1` is now stale relative to `main` and can be discarded when convenient.
- `.orchestrate/wt/coverage` and `.orchestrate/wt/coverage-audit` worktrees (dispatch + throwaway Sol-audit) were both removed after landing `68037c9`.
